
\begin{tikzpicture}
	\path[mindmap,concept color=black,text=white,every annotation/.style={fill=orange}]
	node[concept] (root) {\textbf{Lanceur de billes}}
	[clockwise from=0]
	child[concept color=red] { 
		node[concept] (billes) {Billes}
		node[annotation,right,text=black] at (billes.south west) {FP1};
	}
	child[concept color=red] { 
		node[concept] (cible) {Cible} 
		node[annotation,right,text=black] at (cible.south west) {FP1\\FC5};
	}
	child[concept color=blue] { 
		node[concept] (user) {Utilisateur}
		node[annotation,right,text=black] at (user.south west) {FC1};
	}
	child[concept color=blue] { 
		node[concept] (sec) {Sécurité} 
		node[annotation,right,text=black] at (sec.north west) {FC2};
	}
	child[concept color=blue] { 
		node[concept] (android) {Périphérique android} 
		node[annotation,right,text=black] at (android.north west) {FC3};
	}
	child[concept color=blue] { 
		node[concept] (ener) {Énergie} 
		node[annotation,right,text=black] at (ener.north west) {FC4};
	};

	\path (root) to[circle connection bar switch color=from (black) to (red)] (billes);
	\path (root) to[circle connection bar switch color=from (black) to (red)] (cible);
	\path (root) to[circle connection bar switch color=from (black) to (blue)] (user);
	\path (root) to[circle connection bar switch color=from (black) to (blue)] (sec);
	\path (root) to[circle connection bar switch color=from (black) to (blue)] (android);
	\path (root) to[circle connection bar switch color=from (black) to (blue)] (ener);
	\path (billes) to[circle connection bar switch color=from (red) to (red)] (cible);
\end{tikzpicture}

\begin{tabular}{|l|c|}
	\hline
	FP1 & Lancer la bille sur la cible \\
	\hline
	FC1 & Allumage extinction \\
	\hline
	FC2 & Ne pas tirer sur un humain \\
	\hline
	FC3 & Communiquer par bluetooth \\
	\hline
	FC4 & Utiliser une source d'énergie portable \\
	\hline
	FC5 & Capter la distance à la cible \\
	\hline
\end{tabular}

